# OS
.DS_Store
Thumbs.db

# Python / Django
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
*.sqlite3
db.sqlite3
staticfiles/
mediafiles/

# Env
.env
.env.local
.env.*.local

# Editors
.idea/
*.swp
.vscode/

# Node (if used for tooling)
node_modules/

# Claude local-only settings
.claude/settings.local.json

# Compiled CSS
static/css/output.css

# Locale compiled files
*.mo

# Coverage
.coverage
htmlcov/
.pytest_cache/

# Mypy
.mypy_cache/

# Ruff
.ruff_cache/

# Editable install metadata
*.egg-info/
Screenshot*.png
.superpowers/
.claude/scheduled_tasks.lock
img*.jpeg

# Handbook pipeline outputs (regenerated by `make handbook`)
docs/handbook/handbook.html
docs/handbook/handbook.pdf
docs/handbook/screenshots/curated/
docs/handbook/screenshots/raw/
docs/handbook/flows/_output/
docs/handbook/flows/_storage_states/

# Operator-local pilot data — contains real phone numbers + plaintext
# passwords from `import_whatsapp_roster --magic-links-out`. Never commit.
docs/runbooks/roster_pilot.xlsx
docs/runbooks/*pilot*.csv

# msys / git-bash crash dumps
*.stackdump

# Claude Code /init session transcripts dumped to repo root
*-command-message*.txt

# Private roster data — REAL NAMES of living alumni. This repo is PUBLIC.
# Never commit the class rosters, the WhatsApp roster, or anything derived
# from them. Operators keep these locally and import from their own machine.
*.xlsx
/private-data/

# Security / tech-debt audits. These enumerate OPEN weaknesses with exact
# file:line references and reproduction detail. This repo is PUBLIC, so
# committing them would publish a roadmap of what is still soft on a live
# site. Keep them local; revisit once the remaining findings are closed.
CODE_REVIEW.md
TECH_DEBT_AUDIT.md
